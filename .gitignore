
# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Node / Vite
node_modules/
dist/
!web_app/frontend/dist/
!web_app/frontend/dist/**
.vite/
*.tsbuildinfo

# Claude Code 本機設定（不共用）
.claude/settings.local.json

# 測試用 EDB 專案（大型二進位、可重新產生，不進版控）
*.aedb/
*.aedt
*.aedt.lock
*.aedtresults/

# 除錯用臨時腳本與 AEDT 匯出的參考 schema（不進版控）
test_*.py
!web_app/backend/tests/test_segment_geometry.py
!web_app/backend/tests/
!web_app/backend/tests/test_*.py
aedt_setup*.json

# 執行產物與本機工具目錄（可重新產生，不進版控）
*.log
.codex/
.codex_tmp/
Example_SYZ_Cutout_segments/

# 模擬輸出（含客戶板資料，絕不進版控）
Result/

# 在 repo 根目錄跑工具時留下的本機求解輸出。
#
# 這些原本只靠 CI 攔（禁止檔型與客戶關鍵字兩條規則），但那是最後一道防線，
# 一個手滑的 `git add -A` 就會把整批求解結果送進暫存區。改在這裡先擋掉。
#
# 全部用 `/` 錨定在根目錄：validation/ 底下有 78 個要進版控的驗證 Touchstone，
# 若寫成 *.s2p／*.s4p 之類的通用規則，未來新增的驗證資料會被靜默忽略。
/Example_SYZ.s4p
/Example_SYZ.s2p
/Example_SYZ_cascade_summary.json
/Example_SYZ_segments/
/Example_SYZ_Cutout_ports_segments/
/ibis_channel_runs/
/ami_channel_runs/
/IBIS_Example/

# 一鍵報告的執行期工作區一定含客戶名稱欄位，任何層級都不進版控。
report_workspace/

# OS
Thumbs.db
.DS_Store
